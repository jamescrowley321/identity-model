# Shared node-oidc-provider configuration.
# Copy to .env to override defaults: cp .env.example .env

# Port the provider listens on (mapped to 127.0.0.1).
PORT=9000

# Issuer / base URL advertised in discovery. Must match how clients reach it.
ISSUER=http://localhost:9000
