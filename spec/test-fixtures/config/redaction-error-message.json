{
  "description": "CFG-403: error messages name logical keys and codes, never configured values (secret or not)",
  "mode": "strict",
  "group": "client",
  "client_auth_method": "client_secret_basic",
  "sources": [
    {
      "name": "env",
      "type": "env",
      "values": {
        "OIDC_DISCOVERY_URL": "not-a-url-SENTINEL",
        "OIDC_CLIENT_ID": "id-SENTINEL",
        "OIDC_CLIENT_SECRET": "secret-SENTINEL"
      }
    }
  ],
  "expect": {
    "error": { "code": "CFG-003", "keys": ["client.discovery_url"] },
    "rendered_includes": ["CFG-003", "client.discovery_url"],
    "rendered_excludes": ["not-a-url-SENTINEL", "id-SENTINEL", "secret-SENTINEL"]
  }
}
