{
  "description": "CFG-106: a missing required key (CFG-001) and an invalid value (CFG-003) are collected and reported together, registry-ordered",
  "mode": "strict",
  "group": "client",
  "client_auth_method": "none",
  "sources": [
    {
      "name": "env",
      "type": "env",
      "values": { "HTTP_TIMEOUT": "nope", "OIDC_CLIENT_ID": "spa-client" }
    }
  ],
  "expect": {
    "error": {
      "errors": [
        { "code": "CFG-003", "keys": ["http.timeout"] },
        { "code": "CFG-001", "keys": ["client.discovery_url"] }
      ],
      "ordered_by": "registry order"
    }
  }
}
