{
  "description": "CFG-103: complete client group except client.secret, with a secret-requiring auth method",
  "mode": "strict",
  "group": "client",
  "client_auth_method": "client_secret_basic",
  "sources": [
    {
      "name": "env",
      "type": "env",
      "values": {
        "OIDC_DISCOVERY_URL": "https://issuer.example.com/.well-known/openid-configuration",
        "OIDC_CLIENT_ID": "confidential-client"
      }
    }
  ],
  "expect": {
    "error": { "code": "CFG-002", "keys": ["client", "client.secret"] }
  }
}
